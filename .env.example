# Portfolio Website Environment Variables

# Admin authentication key (REQUIRED for admin access)
# Set this to a strong, random string
ADMIN_KEY=your-super-secret-admin-key-here

# Server port (optional, defaults to 3000)
PORT=3000

# Session secret for cookie encryption (optional but recommended for production)
# Generate a random string for production
SESSION_SECRET=your-session-secret-here

# Node environment (development or production)
NODE_ENV=production
